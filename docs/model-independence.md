# Model independence

The same 200 questions rerun with GPT-5.4 and Kimi K2.6 as the base model
driving both Agent U and Agent G, to test whether the failure class and
the catch rate generalize beyond a single model.

Stub at repo init.
